
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
[tool.dagster]
modules = [{ type = "module", name = "parking.definitions" }, { type = "module", name = "exchange_rates.definitions" }]